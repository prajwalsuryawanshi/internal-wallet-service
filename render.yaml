# Render: Single-service deploy (uses SQLite, no Postgres needed)
# Connect repo → New Blueprint → select this file.
# For Postgres: add a database in Render Dashboard and set DATABASE_URL env var.

services:
  - type: web
    runtime: docker
    name: internal-wallet-service
    region: oregon
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
